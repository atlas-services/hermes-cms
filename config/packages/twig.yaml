twig:
    file_name_pattern: '*.twig'
    paths:
        '%kernel.project_dir%/templates/bundles/HermesBookingBundle': HermesBooking
    form_themes:
        - 'form/fields.html.twig'

when@test:
    twig:
        strict_variables: true
