# yaml-language-server: $schema=../vendor/symfony/dependency-injection/Loader/schema/services.schema.json

# This file is the entry point to configure your own services.
# Files in the packages/ subdirectory configure your dependencies.
# See also https://symfony.com/doc/current/service_container/import.html

# Put parameters here that don't need to change on each machine where the app is deployed
# https://symfony.com/doc/current/best_practices.html#use-parameters-for-application-configuration
imports:
- { resource: 'hermes_templates.yaml'}
- { resource: 'hermes_configs.yaml'}
- { resource: 'hermes_api.yaml'}
parameters:
    app.menu.max_depth: '%env(APP_MAX_DEPTH)%'
    hermes_admin_email: '%env(APP_HERMES_ADMIN_EMAIL)%'
    hermes_admin_password: '%env(APP_HERMES_ADMIN_PASSWORD)%'
    hermes_newsletter_email: '%env(APP_HERMES_NEWSLETTER_EMAIL)%'
    version: v1.0.0
    app.locales: [fr, en] #'%env(APP_LOCALES)%'
    app.default_locale_fallback: fr
    app.default_locale: '%env(default:app.default_locale_fallback:APP_DEFAULT_LOCALE)%'
    app.name: '%env(APP_NAME)%'
    hermes_pages: '%env(APP_PAGES)%'
    hermes_list_templates: '%env(APP_LIST_TEMPLATES)%'
    hermes_list_type_image: '%env(APP_LIST_TYPE_IMAGE)%'
    hermes_database_dir: '%env(DATABASE_URL)%'
    hermes_database_config_dir: '%env(DATABASE_CONFIG_URL)%'
    hermes_path_content_image: '%env(APP_PATH_CONTENT_IMAGES)%'
    hermes_path_content_image_post: '%env(APP_PATH_CONTENT_IMAGES_POSTS)%'
    # Absolute directory for admin Uppy uploads (same tree as Vich content images).
    hermes_admin_media_upload_dir: '%kernel.project_dir%/public/%env(APP_PATH_CONTENT_IMAGES_POSTS)%'
    hermes_path_content_images: '%env(APP_PATH_CONTENT_IMAGES)%'
    hermes_path_cache_image: '%env(APP_PATH_CACHE_IMAGES)%'
    hermes_path_hermes_images: '%kernel.project_dir%/public/%env(APP_PATH_HERMES_IMAGES)%'
services:
    # default configuration for services in *this* file
    _defaults:
        autowire: true      # Automatically injects dependencies in your services.
        autoconfigure: true # Automatically registers your services as commands, event subscribers, etc.

    # makes classes in src/ available to be used as services
    # this creates a service per class whose id is the fully-qualified class name
    App\:
        resource: '../src/'

    # add more service definitions when explicit configuration is needed
    # please note that last definitions always *replace* previous ones
    app:init-hermes:
        tags: ['console.command']
        class: App\Command\InitHermesTemplatesAndConfigCommand
        autowire: true
        public: true

    App\Service\MenuManager:
        arguments:
            $maxDepth: '%app.menu.max_depth%'

    App\Service\HermesApiClient:
        arguments:
            $hermesApiBaseUri: '%hermes_api.base_uri%'
            $hermesApiTemplatesUri: '%hermes_api.templates_uri%'
            $hermesApiLibreCatalogPath: '%hermes_api.libre_catalog_path%'
            $hermesApiCatalogEntity: '%hermes_api.catalog_entity%'
            $hermesApiEmail: '%env(API_HERMES_EMAIL)%'
            $hermesApiPassword: '%env(API_HERMES_PASSWORD)%'
            $hermesApiNotJwtVersion: '%env(bool:API_HERMES_NOT_JWT_VERSION)%'
            $hermesApiIsCms: '%env(bool:API_HERMES_IS_CMS)%'
