{{ include('admin/_delete_form.html.twig', {
    action: path('menu_delete', { id: menu.id }),
    csrf_token_delete: csrf_token('delete_' ~ menu.id),
    confirm_message: 'menu.confirm_delete'|trans({ '%name%': menu.name }),
}) }}
